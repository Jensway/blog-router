{
  "name": "share-center-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fix:android-encoding": "node scripts/fix-android-encoding.js",
    "cap:android": "npm run build && npx cap sync android",
    "android": "npm run cap:android && npx cap open android"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "capacitor": {
    "appId": "com.sharecenter.app",
    "appName": "共享中心",
    "webDir": "dist"
  }
}
